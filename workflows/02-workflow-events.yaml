name: 02 - Workflow Event

on: push

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "I ha've been triggered by a(n) ${{github.event_name}} event."
